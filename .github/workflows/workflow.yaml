
name: 'Terraform CI'

on:
  workflow_dispatch:
  push:
    branches:
    - main
    inputs:
      test-run:
        description: 'Test (y/n)?'
        required: true
        default: 'y'

jobs:
  runs-on: ubuntu-latest
  build:
    steps:
      - name: Yes branch
        if: github.event.inputs.test-run == 'y'
        run: echo "Test = Y"