
name: 'Terraform CI'

on:
  workflow_dispatch:
    inputs:
      test-run:
        description: 'Test (y/n)?'
        required: true
        default: 'y'

jobs:
  build:
    steps:
      - name: Yes branch
        if: github.event.inputs.test-run == 'y'
        run: echo "Test = Y"